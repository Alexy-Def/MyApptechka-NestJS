export enum ENVIRONMENT_NAME {
  LOCAL = 'local',
  DEVELOP = 'develop',
  MASTER = 'master',
  PRODUCTION = 'production',
  TEST = 'test',
}

export enum REQUEST_CONTEXT_TYPE {
  HTTP = 'http',
  RPC = 'rpc',
  WS = 'ws',
}
